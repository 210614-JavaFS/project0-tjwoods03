package com.revature.actions;

public class Transfer {

	//example on how levels of access may work
	//if(user.type.equals("customer") && account.owner.equals(user)) { //do thing} else { syso("You can't do that!"}
	
}
